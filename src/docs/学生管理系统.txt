使用到的技术：
1.登录界面利用计时器轮播图片
2.正则表达式验证输入正确性
3.MD5加密密码进行存储
4.MyBatis简化数据库的增删改查
5.使用HSSFWorkbook导入导出Excle
6.数据库等IO读取都使用了工厂或单例的模式

开发历程：
2022.2.23：
收到老师需要开发gui管理系统的指示。
之前没用Java做过Gui，只用C++做过，Java只做过Web开发。
然后花了大概一下午，熟悉了基本的Swing语法规范。
2022.2.24：
做了个登录界面，以及利用计时器实现了壁纸的轮播。
2022.2.25~2.27：
把基本功能全体实现，包括数据库的增删改查，以及数据输入用正则来验证。
然后简单的界面也成型了，密码采用MD5加密，数据支持Excle和txt文件的导入和导出。
最后再做了一些架构上的优化。
2022.2.28：
美化登录后的mainwindow界面。
并添加了很多标识自我的链接，这里需要重写一个JLable类来达到效果。

(┬┬﹏┬┬) 不得不说，前端的优化过程真的对Swing来说太过艰难